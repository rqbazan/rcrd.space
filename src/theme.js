const breakpoints = {
  sm: 0,
  md: 737,
  lg: 1025
}

const colors = {
  selection: '#00fff2',
  bgColor: '#f2f2f2',
  white: '#fff',
  accent: '#fcd303',
  dark: '#000',
  body: '#474747'
}

const sizes = {
  headerHeight: 56,
  headerPadding: 24
}

const stacks = {
  coverageImg: -1,
  header: 1,
  menu: 2,
  nprogress: 99
}

export default {
  breakpoints,
  colors,
  sizes,
  stacks
}
